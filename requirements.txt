# SHOPCRM — runtime-зависимости (единый список для всего monorepo).
# Правильная установка пакетов: pip install -e packages/core -e apps/bot

aiogram>=3.29
sqlalchemy>=2.0
aiosqlite>=0.20
asyncpg>=0.30
alembic>=1.14
pydantic>=2.0
pydantic-settings>=2.0
loguru>=0.7
deep-translator>=1.11
aiofiles>=24.1
redis>=5.0
